# GitHub Pages — retired

The public demo is the Vercel app:

**https://data-room-coral.vercel.app/**

Do not re-enable Actions Pages or publish `demo/` to `gh-pages`.
Those links were the old static walkthrough. The Next.js app on `main`
is the site (mock auth, admin analytics, theme toggle, documents).

`demo/` stays in the repo as a local HTML snapshot for tests. It is not
a second public host.
