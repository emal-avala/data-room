# Documents on disk

| File | Served as | Notes |
|------|-----------|-------|
| `sample-pitch-deck.html` | `/docs/pitch-deck` via `/api/docs/pitch-deck/deck` | Acme ten-slide deck + theme. Do not move into `public/`. |
| `sample-cap-table.pdf` | unused placeholder | Cap table is now the NDA-gated article at `/docs/cap-table`. |

Edit `src/lib/documents.ts` when you add a slug. See `docs/guides/add-document.md`.
